/** Wraps crawlable Learn SEO intros above the full-height reader/browse shell. */
export function LearnSeoShell({ children }: { children: React.ReactNode }) {
  return <div className="learn-seo-shell">{children}</div>;
}

export function LearnSeoMain({ children }: { children: React.ReactNode }) {
  return <div className="learn-seo-main">{children}</div>;
}
